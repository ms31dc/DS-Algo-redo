1. To calculate the sum uptil N number use the formnula
sum = n*n+1 divide by 2

if you find the sum of n numbers and if given a array then you can subtract from the total sum of that array and your sum to find missing number
https://practice.geeksforgeeks.org/problems/missing-number-in-array/0

public static void missing(int a[])
	{
	    
	    int last=a[(a.length)-1];
	    int number = (last*(last+1))/2;
	    
	    int sum=0;
	    for(int i=0;i<a.length;i++)
	    {
	        sum=sum+a[i];
	    }
	    
	    System.out.print(number-sum);
	    
	}
